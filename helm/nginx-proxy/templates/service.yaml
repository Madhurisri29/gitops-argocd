apiVersion: v1
kind: Service
metadata:
  name: nginx-proxy
  namespace: proxy
  labels:
    {{- include "nginx-proxy.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    id: C0910893
